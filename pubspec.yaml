name: medicinnect
description: MediCinnect - Patient Mobile Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.0
  cloud_firestore: ^5.4.0
  google_sign_in: ^6.2.1
  provider: ^6.1.1
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
  table_calendar: ^3.1.2
  cached_network_image: ^3.4.1
  flutter_svg: ^2.0.9
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
